# Desktop UI and numerics - required.
PyQt6>=6.5
numpy>=1.24

# Document ingestion: PDF text layer and page rendering for OCR - required.
pypdfium2>=4.0

# sentra.py: the local SQL database and vector index (SQLite built in; add a
# driver such as psycopg for a shared PostgreSQL server), encrypted backups and
# the S3 / WebDAV backup targets.
SQLAlchemy>=2.0
cryptography>=41
requests>=2.31

# Semantic encoder (stage 2). Pulls torch and transformers, ~2 GB installed, and
# downloads all-MiniLM-L6-v2 (~90 MB) on first run. Remove this line to run the
# console offline: it falls back to the deterministic lexical engine, which needs
# no model, no network and no torch.
sentence-transformers>=3.0

# MLOps: supervised model plus experiment tracking. Without them the console runs
# on the rule and semantic paths only, and the Settings tab says so.
xgboost>=2.0
scikit-learn>=1.3
mlflow>=2.9

# OCR for scanned reports and photographs. Optional: PDFs with a text layer are
# read without it. paddlepaddle is the runtime paddleocr needs, and the detection
# and recognition models download on first use.
paddleocr>=2.7
paddlepaddle>=2.6
